# Source changelist: what the review found on the release

**Date:** 2026-09-02
**Phase:** IMPL
**Doc changelist:** `202609022245_changelist.doc.lock.md`

## `homma-core`

### `src/release/publish.rs`, `publish_tests.rs` (modify)

CHANGE: `publish_jsr` runs `deno publish --token <t>` without `--allow-dirty`, and a failed run's `PublishError::Failed` carries the command line and log with the token replaced by `<token>`; the module doc names jsr as the one tool that takes its token on the arguments. The test that asserted the token in the recorded line asserts the redaction in the error instead.

### `src/release/run.rs` (modify)

CHANGE: `step_merge_and_tag` aborts a merge in progress and switches back to the trunk before returning any error raised after the switch to the release line, so a failed release leaves the tree on the branch it started from. `run_tests.rs` plants a push that fails and asserts the branch and a clean tree.

## The root `README.md` (modify)

CHANGE: the command table gains the `homma release <op>` row, which `tests/the_readme_examples_are_real.rs` reads against the command tree.
