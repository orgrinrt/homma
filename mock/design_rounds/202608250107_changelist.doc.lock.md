# DOC changelist: the root design document names the launcher

**Phase:** DOC

One sentence, in one file.

## `mock/DESIGN.md.tmpl`

`## What exists` enumerates the crates and says `homma` is the command surface.
It is the launcher. `homma-engine`, which the enumeration does not mention at
all, is the command surface.

Both go in, because a reader counting crates against the list finds one missing
either way.

## Lock criteria

- The sentence names the launcher and the engine for what each is.
- `homma-engine` appears in the enumeration.
- `docs/DESIGN.md` regenerates from it and carries the same sentence.
