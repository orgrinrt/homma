# Doc changelist: the exit code is the contract

## Every document

No change. What the two commands are, that `check` writes nothing and exits
non-zero when something blocks, and that the gate reads that exit code, are all
stated already, in `mock/DESIGN.md.tmpl` under the command surface and in
`mock/crates/homma-org/DESIGN.md.tmpl` under the shared tool configs.

This round adds a test for a contract the design already states. Restating it
would be a second copy to disagree with the first, and documenting that
something is now tested is a changelog entry rather than a design.
