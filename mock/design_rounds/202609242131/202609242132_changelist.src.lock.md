# source changelist

Implements `202609242131_changelist.doc.lock.md`.

## CHANGE: module `capture::typed` added
File: `mock/crates/homma-engine/src/cmd/capture/typed.rs`
Verification: `Typed { at, text }`, `records(transcript_path)` reading
`<session>.typed-by-tools.ndjson` beside it (absent is none, a malformed line is
refused by number), and `without(events, records)` dropping one said event per
record. Tests: bare and wrapped matches, a record before the line's stamp, the
person's same words later kept, an answered round never dropped.

## CHANGE: fn `capture::run` FROM writing unchecked TO checking the store against `Denied::for_the_workspace`, and dropping typed lines
File: `mock/crates/homma-engine/src/cmd/capture/mod.rs`
Verification: smoke tests: `--into` under `$HOME/.claude`, `--into` a denied
directory, and `[paths] captures` a denied directory, each refused with nothing
written; no `HOME` refused for a session by path.

## CHANGE: fn `transcript::round` FROM an event for any round TO none for a round that said nothing
File: `mock/crates/homma-engine/src/cmd/capture/transcript.rs`
Verification: a test over the timeout shape, `afkTimeoutMs` with empty
`answers`, reading no event; a notes-only round still one.

## CHANGE: fn `capture::make` FROM `--bears-on` raw TO refused with a line break
File: `mock/crates/homma-engine/src/cmd/capture/mod.rs`
Verification: a test in `tests/make.rs`.

## Tests and wording

- The mirror in `tests/render.rs` trims and matches `\s` with JavaScript's
  whitespace set; a test pins U+0085 kept and U+00A0 trimmed.
- The answer tests leave `tests/transcript.rs` for `tests/answers.rs`, and the
  remaining pronouns and the process word go.
- The test that counted a `/compact` line as the person's asserts a slash
  command the person typed is theirs, and the typed-by-tools tests carry the
  injected one.

## Outside this repository

The workspace's self-compact injector writes the record, in its own pull
request against the workspace.
