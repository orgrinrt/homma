# Doc changelist: no document changes

**Date:** 2026-08-24
**Phase:** DOC

The pin lives in `homma-core/Cargo.toml` and no per-crate document states it.
`homma-core` ships no design document at all, which is recorded separately and
is not this round's to fix.
