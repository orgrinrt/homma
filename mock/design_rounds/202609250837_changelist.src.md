# Src changelist: a tool's paste with line breaks round it

Doc changelist: `202609250836_changelist.doc.lock.md`.

## CHANGE: fn `unwrapped` FROM matching a wrapper that opens and closes the words exactly TO also taking off line breaks outside it
File: `mock/crates/homma-engine/src/cmd/capture/typed.rs`
Verification: `grep -c "trim_matches('\\\\n')" mock/crates/homma-engine/src/cmd/capture/typed.rs` returns 1, and
`cargo test --manifest-path mock/Cargo.toml -p homma-engine capture::tests::typed` passes.

## Tests

`mock/crates/homma-engine/src/cmd/capture/tests/typed.rs` gains two arms.
`a_paste_with_line_breaks_round_its_wrapper_is_the_tools` is the shape the
injector's line landed in, and fails with the trim taken out.
`only_line_breaks_outside_the_wrapper_are_the_harnesss` holds a space, a tab,
trailing words, line breaks round a bare text, and a line break inside the
wrapper as the person's, and fails with the trim widened to all whitespace.

## Open topics in this tree

`202609242357_topic.capture-answers-left-on-a-page.md` needs no source change
here, as the doc changelist says, and is re-filed after close.
