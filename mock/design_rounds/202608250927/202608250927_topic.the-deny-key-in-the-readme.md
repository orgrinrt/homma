# Topic: the deny key, in the readme

**Date:** 2026-08-25
**Scope:** `README.md`, and the formatting the previous round's own gate left
behind.

The manifest gained a `deny` key: the places homma may not write, beyond the
two it derives. An operator who does not know it exists cannot use it, and the
readme is where they would look, so it goes in the manifest example beside the
keys that are already there.

The second half is smaller and worth saying plainly rather than folding in
silently. The commit that closed the previous round ran the autofix, which
reformatted four source files, and the round was already archived by the time
that landed. So four whitespace deltas sit in the tree with no round open to
carry them. They are what `rustfmt` wanted of lines this branch wrote; nothing
in them is a change of behaviour.
