# Doc changelist: no document changes

No `*.md.tmpl` is touched by this round.

The round adds one test and one ignore list, neither of which is a design
surface a document describes. `DESIGN.md.tmpl` names the crates and what each
is for, and gains nothing from a repository's ignore configuration;
`WORKFLOW.md.tmpl` names the flow a contributor follows, and a contributor does
not maintain this by hand.

The changelist exists because the phase machine wants one, and saying that
plainly is better than inventing a paragraph for a document so it has something
to point at.
