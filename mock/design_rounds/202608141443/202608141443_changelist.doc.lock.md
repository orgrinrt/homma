# DOC changelist: containment is a property of the filesystem

**Round:** the filesystem round
**Phase:** DOC

## CHANGE: mock/crates/homma-org/DESIGN.md.tmpl

Say that containment is asked of the filesystem rather than computed from the
path, that the path is resolved as far as it exists before anything walks it,
and that a bare repository is a repository.

Say that every path written to is checked, not the one a reproduction pointed
at. Moving the check from the root to the workspace put the original defect back
on the root, which is where the directories and definitions land.
