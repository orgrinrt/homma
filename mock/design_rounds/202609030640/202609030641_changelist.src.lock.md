# Source changelist: a hooks path at the repo root is tracked

**Date:** 2026-09-03
**Phase:** IMPL
**Doc changelist:** `202609030640_changelist.doc.lock.md`

## `homma-core`

### `src/release/hook.rs` (modify)

CHANGE: the pathspec handed to `git ls-files` is `.` where the hooks directory is the root itself, so a root hooks path is refused as tracked rather than failing on an empty pathspec.

## `homma-engine`

### `tests/cli_release.rs` (modify)

CHANGE: the refusal smoke test plants `core.hooksPath = .` and asserts the tracked-directory line.
