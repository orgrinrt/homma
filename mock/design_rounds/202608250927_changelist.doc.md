# Doc changelist: the deny key, in the readme

**Date:** 2026-08-25
**Round:** 202608250927
**Source topic:** `202608250927_topic.the-deny-key-in-the-readme.md`

## CHANGE: `README.md`

The manifest example gains `deny`, in both spellings it takes: a bare path, and
a table carrying the reason the refusal will show. The paragraph under the
example says what the key is for and what is denied without being named, so the
list an operator is adding to is visible rather than implied.
