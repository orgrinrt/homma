# Doc changelist: the enumeration's first bullet was the same overstatement

## CHANGE: `homma-org/DESIGN.md.tmpl`, bullet one of the proven-surface list

The previous round replaced a claim of totality with an enumeration of what is
proven, and led that enumeration with "a write computed from an unchecked path
does not compile". A reviewer compiled a counterexample in one line.

The replacement states what actually holds, which is narrower and checkable:
two functions take a proven path and therefore cannot be called without one. It
also states the argument for the `create_dir_all` window that has been carried
open for three rounds and never written down.

## CHANGE: `README.md`, the Status section

It describes homma as carrying a registry of the participants and does not
mention standing one up, which is the surface eleven review rounds have been
about.
