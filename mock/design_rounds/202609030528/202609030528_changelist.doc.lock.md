# Doc changelist: the test named for the exit code never read one

## Every document

No change. `mock/DESIGN.md.tmpl` already states the three codes and what the gate
does with each, from round `202609030003`; this round makes a test assert what
that document already says. Restating it would be a second copy to disagree with
the first, and saying that something is now tested is a changelog entry rather
than a design.
