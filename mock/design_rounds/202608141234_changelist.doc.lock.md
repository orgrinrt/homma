# DOC changelist: the guard nothing tested

**Round:** the guard round
**Phase:** DOC

## CHANGE: mock/crates/homma-org/DESIGN.md.tmpl

Say that two repositories are the same when their host, owner and name agree, not
when their last path segment does. A fork and its upstream share a name and are
not the same repository, and that is the ordinary case rather than an exotic one.

Say that `local` refuses a directory sitting inside another repository's working
tree, rather than initialising a nested one there.
