# DOC changelist: the mechanism was built and never wired

**Round:** the wiring round
**Phase:** DOC

## CHANGE: mock/crates/homma-org/DESIGN.md.tmpl

State where the clone URL comes from, which is the workspace root's own `origin`
rather than a configuration key, and why: a second place for the same fact is a
second place for it to be wrong.

The paragraph asserting that the identity is set after standing up stays and
becomes true in this round. It was written a round early.

## CHANGE: mock/crates/homma-api/DESIGN.md.tmpl

Rename the vocabulary from standing to staffing throughout, since *standing* is
already what a reference derives in this same crate.
