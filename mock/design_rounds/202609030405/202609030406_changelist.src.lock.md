# Source changelist: the release errors fit the gate it runs

**Date:** 2026-09-03
**Phase:** IMPL
**Doc changelist:** `202609030405_changelist.doc.lock.md`

## `homma-core`

### `src/release/plan.rs` (modify)

CHANGE: the off-level payload is a struct `OffLevel { manifest, last, level, next }` and the variant is `PlanError::OffLevel(Box<OffLevel>)`, so `PlanError` and every result carrying it fit under clippy's large-error limit; the display and the tests read through the box.

### `src/release/git.rs` (modify)

CHANGE: `remote_tags` carries, beside the http low-speed bound, `GIT_SSH_COMMAND` with a connect timeout and keepalives where the caller's environment does not already set one, and the comment names which transport each bound covers. `reset_hard` and `abort_merge` each carry their own doc comment.

## `homma-engine`

### `src/cmd/release/mod.rs` (modify)

CHANGE: the sweep's match on the off-level error follows the boxed shape.
