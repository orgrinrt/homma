# SRC changelist

**Round:** the pin round
**Phase:** IMPL

## CHANGE: mock/crates/homma-api/src/path.rs

Two tests. One pins that descending cannot leave, which only `RelPath` was
guarding. One pins that anchoring may, so the split is not closed by symmetry.
