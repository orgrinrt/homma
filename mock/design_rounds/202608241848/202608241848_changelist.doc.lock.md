# Doc changelist: the configs stage has to fit the repo it lands in

No `*.md.tmpl` edits. The change is entirely in source and in the workspace
manifest, which is not a rendered document.

Opened so the round can reach a source window; locked immediately.
