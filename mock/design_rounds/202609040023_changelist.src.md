# Source changelist: the gate tests split by concern

**Date:** 2026-09-04
**Phase:** IMPL
**Doc changelist:** `202609040018_changelist.doc.lock.md`

## `homma-core`

### `src/release/gate_tests.rs` MOVES TO `src/release/gate_tests/mod.rs` (modify)

CHANGE: the table-answering `Fake` runner, `crate_root` and `git_repo_with`
stay in `mod.rs`, which declares `features`, `steps` and `whole`; the
twenty-four tests move into those three files unchanged, each behind
`use super::*`.
Verification: `cargo test -p homma-core release::gate` reports 23 passed and
1 ignored, the 24 the single file carried, and
`grep -c '#\[test\]' mock/crates/homma-core/src/release/gate_tests/*.rs`
sums to 24 against 24 in the file at the previous revision.

### `src/release/gate.rs` (modify)

CHANGE: the `#[path]` on the test module FROM `gate_tests.rs` TO
`gate_tests/mod.rs`.
Verification: the test run above is the module being found; the file-size
lint no longer names `gate_tests.rs` on commit.
