# Doc changelist: what the fourth review found on the release

**Date:** 2026-09-02
**Phase:** DOC
**Source topics:** `202609022332_topic.what-the-fourth-review-found-on-the-release.md`

## Current state

The root `DESIGN.md.tmpl` and `homma-core/README.md.tmpl` name `DEEPDIVE_release.md` as a file, which renders nowhere under that name. `homma-core/DEEPDIVE_release.md.tmpl` says the gate decides what a repo is from its root and says nothing about a repo whose manifest lives below it.

## Changes proposed

### `DESIGN.md.tmpl` (modify)

The release paragraph points at `CORE_RELEASE.md`, which is where the deep dive renders.

### `homma-core/README.md.tmpl` (modify)

The same pointer, `CORE_RELEASE.md`.

### `homma-core/DEEPDIVE_release.md.tmpl` (modify)

The gate paragraph says that a repo keeping its manifests under a subdirectory, `mock/` in this workspace, is read as having none today, that the sweep names such a repo as passed over, and that reading it is owed.
