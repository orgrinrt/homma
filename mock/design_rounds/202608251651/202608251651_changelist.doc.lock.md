# Doc changelist: what a stranger reads

## README.md

Two claims to correct and one contradiction to resolve.

- CHANGE: the usage block names `--remote`, which the command line has never
  taken. The flag is `--forge`. Copying the line as written exits 2.
- CHANGE: the opening paragraph says the manifest names the repositories and
  where each of them lives. It does not, and a later paragraph in the same
  document says so. The opening is the one a reader meets first.
- CHANGE: `local_path` is documented as a manifest key. It is not one, and
  writing it produces a parse failure.
- CHANGE: the manifest section documents no pin key, while the launcher section
  says the launcher reads the engine version the workspace pins. Name the keys.

## mock/agent

No change. The generated agent instructions are not a stranger's surface.
