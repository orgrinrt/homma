# Doc changelist: separate the two halves, and stop flattening a distinction

## CHANGE: `README.md`

**It now says `org add` is unchecked, and it is checked.** The paragraph exists
to draw one distinction and flattens it: a reader takes away that neither writer
is checked when one is checked against the record and the other is not.

**The gen pass disclosure is split in two**, because the justification the
previous round gave covers only half of what it was justifying. `~/Dev/clause-dev`
is the aggregation target and refusing it would forbid the tool its purpose;
`~/.claude` is user scope, is never a target, and is now refused.

**`docs/` does not exist.** The README says design documents are generated into
it from templates under `mock/`, and there is no such directory tracked or
untracked, and no rendered file beside any template. Pre-existing, and a prior
round in this repository was titled `the-readme-describes-a-repo-that-no-longer-exists`.

## CHANGE: `mock/crates/homma-org/DESIGN.md.tmpl`

**"a property of one subcommand" is stale as of the previous round's own
change**, which gave `org add` a deny check. It is two, and now three.

**The residual is understated.** It names Unicode normalisation as the only gap;
two further classes are case rather than normalisation and are closed here, so
the sentence would have been wrong in a direction that reads as reassuring.
