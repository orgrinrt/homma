# doc changelist: the readme test asks the binary that answers

## Current state

`mock/DESIGN.md.tmpl` says nothing about `the_readme_examples_are_real` or about
the readme check generally. The launcher's own command is described in
`launcher/src/workspace/mod.rs` and in the readme's table row, and both already
say it is the launcher's, answered without the engine.

## Changes proposed

None. The fix is to two test files and one dev-dependency, and the design has
no statement about either binary's test suite for them to disagree with. The
topic carries the reasoning, and the src changelist carries the claims.
