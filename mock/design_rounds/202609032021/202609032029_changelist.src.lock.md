# Source changelist: the description follows the tagline

**Date:** 2026-09-03
**Phase:** IMPL
**Doc changelist:** `202609032021_changelist.doc.lock.md`

## `homma-api`

### `src/release.rs` (modify)

CHANGE: `Step` gains `Description`, last in `ALL`, blocking, named
`description`.
Verification: `grep -c "Step::Description" mock/crates/homma-api/src/release.rs` is at least 3.

## `homma-core`

### `src/release/tagline.rs` (add)

CHANGE: `tagline(readme)` is the first paragraph of prose after the title,
past a `<div>` block, badge rows and blank lines, joined over its lines;
`cargo_description(root)` reads `[package].description` or
`[workspace.package].description`; `deno_description(root)` reads
`deno.json`'s; `check(root, kind)` is the step's outcome: each manifest the
kind names against the tagline trimmed at the ends, a difference or a
missing description failing with both strings in the log, no tagline a skip
that says so, no manifest for the kind a plain skip. Tests pin every shape
of readme the estate has, the two manifests, the trailing full stop, and
the skips.

### `src/release/gate.rs`, `src/release/mod.rs` (modify)

CHANGE: `run_step` answers `Step::Description` from `tagline::check` before
asking for calls; `calls_for` has the arm and makes none; the module says
seven steps.
