# DOC changelist

No document changes.

No `*.md.tmpl` mentions `token_env`, so none describes where a credential comes
from. Checked rather than assumed. The generated structure documents carry
crate names and layers, which this does not move.

The window opens and closes empty so the round walks its phases in order rather
than around them.
