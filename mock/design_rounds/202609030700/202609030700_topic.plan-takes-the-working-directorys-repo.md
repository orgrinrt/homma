# Plan takes the working directory's repo

**Date:** 2026-09-03
**Phase:** TOPIC
**Scope:** one line of `homma-core/DEEPDIVE_release.md.tmpl`, `homma-engine/tests/cli_release.rs`
**Source topics:** the review of pull request 50, and `202609030650/202609030650_topic.the-hook-takes-gits-arguments-and-runs-from-a-worktree.md`

## The findings

The last round made the plan's repo optional and said no template was touched, while the design's synopsis kept the repo mandatory for the plan alone. The synopsis brackets it, as it does for the check and the run.

The invocation the last round repaired, `gate --hook` with git's two arguments and the refs on stdin from a worktree, was asserted by no test in that composition, and nothing checked that the positional under `--hook` is not read as a repo. A smoke test runs it from a worktree with no refs on stdin and reads the nothing-to-gate line, and runs it from a stray directory with a positional that is a real repo's name and reads the refusal, which is the positional not being read.

A push from a worktree whose tip is the clone's own head gates in the clone, which refuses where another seat's tree is dirty; filed for its own round.

## What does not change

The hook script and the three refusals.
