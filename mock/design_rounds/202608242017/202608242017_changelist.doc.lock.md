# DOC changelist: 202608242017

No document changes.

The trait gains a method and one client gains a redirect setting. Neither
appears in any `*.md.tmpl`: `Forge` is described in its own rustdoc, and the
generated structure documents carry crate names and layers rather than trait
surfaces. Checked rather than assumed, `grep -l 'Forge\b' mock/**/*.md.tmpl`
returns nothing.

The window is opened and closed empty so the round walks its phases in order
rather than around them.
