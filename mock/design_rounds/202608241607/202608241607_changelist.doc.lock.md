# Doc changelist: the merge git could not finish

**Phase:** DOC
**Round:** 202608241607
**Source topic:** `202608241607_topic.the-merge-git-could-not-finish.md`

No doc template changes. The re-export block is not described anywhere in the
design documents, and the round exists to carry a merge resolution rather than
to change what the crate is.

The doc templates this merge stages (`mock/DESIGN.md.tmpl`,
`mock/crates/homma-org/DESIGN.md.tmpl`) are the merge's own work, inherited
from trunk unchanged, and are excused as such. Nothing here authors them.
