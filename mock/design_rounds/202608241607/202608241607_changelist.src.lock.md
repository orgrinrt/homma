# Source changelist: the merge git could not finish

**Date:** 2026-08-24
**Phase:** SRC
**Topic:** `202608241607_topic.the-merge-git-could-not-finish.md`

## CHANGE: `mock/crates/homma-api/src/lib.rs`

EDIT the re-export block to carry both sides of the merge. Trunk widened
`denied` to `{Denied, Forbidden, NoHome, Standing}`; this branch widened `git`
to `{CommitIdentity, Git}`. The two lines are adjacent, so git reports one
conflicting hunk and cannot combine them, and the resolution takes both.

No symbol is added or removed by this round beyond what the two sides already
added: every name in the block is one trunk or this branch already exported.

This is the only file of the fifty-five the merge stages that is not the
merge's own work, and it is the only one this changelist names.
