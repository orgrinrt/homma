# Src changelist: drop the needless borrow

## CHANGE: `git_impl.rs:136`

`file.raw_value(&format!(...))` becomes `file.raw_value(format!(...))`. The
callee takes `AsRef`, so the borrow is redundant and clippy says so.
