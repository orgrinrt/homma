# Doc changelist: none

**Date:** 2026-08-24
**Phase:** DOC
**Topic:** `202608241128_topic.aggregation-denies-its-own-workspace.md`

No `*.md.tmpl` changes. The design documents describe the deny list as a set of
places nothing may be written under, which stays true: the fix removes an entry
that names the actor's own workspace, and does not change what an entry means.

The window is opened and closed so the round can reach the source phase, which
is what gates the change.

## CHANGE: none
