# Doc changelist: what the second review of the hooks round found

Topic: `202609252350_topic.second-review-of-the-per-clone-hooks-round.md`.

## Current state

`mock/DESIGN.md.tmpl` says a registration of homma's found in the shared
`settings.json` is swept whichever member it names, and does not say how a
registration is known to be homma's.

## CHANGE: the agent hooks section FROM silent on how a registration is homma's TO the `<repo>--<rest>` name under `.claude/hooks/` being homma's namespace, present or not
File: `mock/DESIGN.md.tmpl`

A hand-written hook takes another name or another directory, and a file under
that shape that homma did not write is what keeps a registration. Keeping every
registration whose file is absent instead would keep every stale one of homma's
in a clone that never held the member, which is what the per-clone round
started from.
