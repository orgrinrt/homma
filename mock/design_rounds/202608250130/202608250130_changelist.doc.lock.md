# Doc changelist: 202608250130

## Scope

No design document changes.

## Why nothing

The round adds a copyright notice to source files and a config file naming
which. Neither is a design fact: nothing about what homma is, what it does, or
how its pieces relate moves. `DESIGN.md.tmpl` describes the launcher and the
engine and would say the same thing before and after.

`ante.toml` is not a design document either. It is a tool's configuration,
sitting at the repository root beside `deny.toml` and `rust-toolchain.toml`,
and it is committed under the topic rather than planned here.

## FILES

(none)
