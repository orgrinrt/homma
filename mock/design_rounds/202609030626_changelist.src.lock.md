# Source changelist: the three refusals are asserted through the binary

**Date:** 2026-09-03
**Phase:** IMPL
**Doc changelist:** `202609030625_changelist.doc.lock.md`

## `homma-engine`

### `src/cmd/release/mod.rs` (modify)

CHANGE: `hook_cmd` names `HookError::Git` and `HookError::Io` as the arms that become errors, so a variant added later has no arm and does not compile.

### `tests/cli_release.rs` (modify)

CHANGE: one test plants the three refusals in turn, a hooks path outside the repo, a tracked hooks directory, and a foreign `pre-push`, and asserts each exits non-zero with its line on stdout and writes nothing.
