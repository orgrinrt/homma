# Changelist: doc — the taxonomy

**Round:** 202608140200 crate taxonomy
**Phase:** DOC
**Scope:** the layer labels, and a design document per new crate

## CHANGE: replace the placeholder layers in `mockspace.toml`

`L0 core` / `L1 surface` described the undesigned split the adoption round
inherited. The settled six replace them, and the lints read them.

## CHANGE: `mock/crates/homma-api/DESIGN.md.tmpl`

What a record is, what a kind is, the attribute types that exist now and why the
rest are absent, and the configuration schema. No I/O, so its whole surface is
types and the traits over them.

## CHANGE: `mock/crates/homma-store/DESIGN.md.tmpl`

Append and read over newline-delimited records, per-kind mutability, and what
being the sole writer means. States plainly that correspondence and structural
records share one engine and differ by a flag their kind declares.

## CHANGE: `mock/crates/homma-org/DESIGN.md.tmpl`

The registry, the workspace lifecycle, and the generation pass. This is where
identity becomes a directory on disk with a git config and a linked memory.

## CHANGE: rewrite `mock/DESIGN.md.tmpl`

The adoption round's version described two crates and said the taxonomy round
would replace it. This is that replacement.
