# Doc changelist: what the fifth review found on the release

**Date:** 2026-09-03
**Phase:** DOC
**Source topics:** `202609030005_topic.what-the-fifth-review-found-on-the-release.md`

## Current state

`homma-core/DEEPDIVE_release.md.tmpl` puts `tag.prefix`, `tag.annotated`, `tag.dupes`, `tag.local`, `reg.unpublished` and `both.sameset` in the set a release needs green, and says a repo-less run goes in dependency order. The plan paragraph says nothing about a manifest that already disagrees with the level.

## Changes proposed

### `homma-core/DEEPDIVE_release.md.tmpl` (modify)

The check section says which of those six block and which warn: `reg.unpublished` blocks on the newest tag and warns on older ones, `both.sameset` blocks, and `tag.prefix`, `tag.annotated`, `tag.dupes` and `tag.local` warn. The run section says the plan refuses where the manifest version is neither the last tag's nor what the level makes of it. The repo-less paragraph says the order is by name today, that a run with a dependency edge between two repos it would release is refused, and that dependency order is owed.
