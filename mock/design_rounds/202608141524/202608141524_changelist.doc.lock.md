# DOC changelist: join descends, resolve anchors

**Round:** the join round
**Phase:** DOC

## CHANGE: mock/crates/homma-api/DESIGN.md.tmpl

State the two operations and why they differ: one anchors a path the caller may
legitimately put anywhere and is guarded at runtime against the filesystem; the
other descends into a tree we own and cannot leave it at all.

State that the configured paths carry a type refusing an absolute or escaping
value when the configuration is parsed.
