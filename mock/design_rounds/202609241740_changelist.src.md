# src changelist

Implements the doc changelist `202609241737_changelist.doc.lock.md`: the
`homma capture` command in `homma-engine`, reading a session transcript into one
capture file in the workspace's capture store.

## CHANGE: command `homma capture` FROM absent TO present
File: `mock/crates/homma-engine/src/cli.rs`
Verification: `grep -n "Capture {" mock/crates/homma-engine/src/cli.rs` returns the variant, with `--title`, `--session`, `--since`, `--bears-on` and `--into`.

## CHANGE: module `cmd::capture` FROM absent TO present
File: `mock/crates/homma-engine/src/cmd/capture/mod.rs`
Verification: `grep -n "pub mod capture" mock/crates/homma-engine/src/cmd/mod.rs` returns one match, and `Command::Capture` dispatches to `capture::run`.

## CHANGE: the transcript reader `capture::transcript::read` FROM absent TO present
File: `mock/crates/homma-engine/src/cmd/capture/transcript.rs`
Verification: the tests under `src/cmd/capture/tests/` read typed lines by `origin.kind`, queued-command attachments, answered rounds off `toolUseResult`, and refuse a malformed line of a read shape by number.

## CHANGE: the store side `capture::store` FROM absent TO present
File: `mock/crates/homma-engine/src/cmd/capture/store.rs`
Verification: tests cover the watermark read from `source` and `through`, `--since` against it, the slug, the transcript directory's escaping, and the paths found in the words.

## CHANGE: the file shape `capture::render` FROM absent TO present
File: `mock/crates/homma-engine/src/cmd/capture/render.rs`
Verification: tests cover that only the person's words are blockquotes, that a blockquote strips back to the exact string, the fence outgrowing a backtick run in the text, and every answer kind.

## CHANGE: dependency `jiff` FROM absent TO present
File: `mock/crates/homma-engine/Cargo.toml`
Verification: `grep -n '^jiff' mock/crates/homma-engine/Cargo.toml` returns one match. It converts the transcript's UTC instants to the local time a capture is stamped in.

## CHANGE: readme row for `homma capture` FROM absent TO present
File: `README.md`
Verification: the command table carries a `homma capture` row, which `tests/the_readme_examples_are_real.rs` checks against the binary.
