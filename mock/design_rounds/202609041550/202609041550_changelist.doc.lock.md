# doc changelist

One sentence in the root design template, and the rendered document that
carries it. Descends from the topic file
`202609041549_topic.the-rendered-design-meets-the-voice-gate.md`.

## What lands

- `mock/DESIGN.md.tmpl`, under "What a status answers": "What the plain
  status buys is not having to know which of them to reach for" becomes
  "With the plain status there is no need to know which of them to reach
  for". The same claim without the cost metaphor.
- `docs/DESIGN.md`, rendered from the template, which had not been rendered
  since the template gained the workspace command's two paragraphs in round
  `202609041520`.

## Claims the source changelist will carry

None. The source changelist exists so the round closes, and says so.
