# Source changelist: plan takes the working directory's repo

**Date:** 2026-09-03
**Phase:** IMPL
**Doc changelist:** `202609030700_changelist.doc.lock.md`

## `homma-engine`

### `tests/cli_release.rs` (modify)

CHANGE: a smoke test runs `gate --hook origin <url>` with empty stdin from a worktree of a member and asserts a clean exit with the nothing-to-gate line, and runs the same from a stray directory with a positional naming a real repo and asserts the not-inside refusal, since the positional is git's and not a repo.
