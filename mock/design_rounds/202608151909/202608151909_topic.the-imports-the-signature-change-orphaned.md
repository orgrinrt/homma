# The imports the signature change orphaned

`install_workspace_gate` took a `&Path` and wrote through `std::fs`. It now takes
a `Root` and writes through it, so both imports are dead in the non-test half of
the file and the pre-commit autofix removed them after the previous round had
closed.

A round for two lines, because the alternative is reverting a correct change and
letting the autofix re-apply it on the next commit, which is the same edit
arriving without a record of why.
