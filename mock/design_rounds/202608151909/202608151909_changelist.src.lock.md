# Src changelist: drop the orphaned imports

## CHANGE: `mock/crates/homma/src/cmd/gates.rs`

`std::fs` and `std::path::Path` are unused in the non-test half now that
`install_workspace_gate` takes a `Root` and writes through it. The test module
keeps its own `Path` import.
