# DOC changelist: the precondition has no type

**Round:** the AbsPath round
**Phase:** DOC

## CHANGE: mock/crates/homma-api/DESIGN.md.tmpl

Replace the paragraph saying every path reaching the git contract is absolute
with the statement that it is a **type** which cannot be relative, and say why the
prose version failed: a precondition stated in a comment has to be re-checked at
every call, a precondition checked at runtime in one implementation is not checked
in the other six, and a precondition in the signature is checked by the compiler
once.
