# Source changelist: the hook install refuses a hook that is not its own

**Date:** 2026-09-03
**Phase:** IMPL
**Doc changelist:** `202609030545_changelist.doc.lock.md`

## `homma-core`

### `src/release/hook.rs` (modify)

CHANGE: `HookError` gains `HooksPathTracked(PathBuf)` and `HookExists(PathBuf)`. `install` refuses where any file under the hooks directory is tracked, and where a `pre-push` is already there whose text is not this tool's; its own is rewritten. Tests plant a tracked `.githooks/`, a foreign `pre-push`, and the tool's own, and assert the two refusals name their path and the third writes.
