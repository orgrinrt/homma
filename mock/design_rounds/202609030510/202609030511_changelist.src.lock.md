# Source changelist: --post names a commit the way --sha does

**Date:** 2026-09-03
**Phase:** IMPL
**Doc changelist:** `202609030510_changelist.doc.lock.md`

## `homma-engine`

### `src/cmd/release/gating.rs` (modify), `tests/cli_release.rs` (modify)

CHANGE: `--post` resolves the commit it names before looking for a record, refusing a string that is not a commit; the not-found message carries the full sha. Smoke tests: a string that is no commit is refused naming it, and a short sha of a real commit with no record is reported under its full sha.

## `homma-core`

### `src/release/status.rs` (modify)

CHANGE: the description is cut at a character boundary, with a test planting a multibyte description past the limit.
