# Doc changelist: the list is three of seven, and regen writes executables

## CHANGE: `mock/crates/homma-org/DESIGN.md.tmpl`

**The deny list is described as being what it is a subset of.** Both this
document and `denied.rs` say "three absolute locations", and the record's list
has **seven** items. Four of them are not location-shaped, which is why homma
implements three, but a reader of either document has no way to learn the other
four exist. Corrected to three of seven, with where the rest live and why they
are not this mechanism's.

**The last round's comparison paragraph is wrong in the same way its code was.**
It says the identity comparison and the component comparison cover each other's
gap. They do not cover a denied place that is both absent and spelled with
different case. Rewritten to say what the comparison now does and that it
deliberately over-refuses.

## CHANGE: `README.md`

**The list of writers with no deny enforcement is incomplete, and an incomplete
list of known gaps reads as a complete one.** `cmd/registry.rs` is missing from
it, and the entry for `agent regen` says "no containment story", which
understates it: it writes hook scripts, marks them executable, rewrites
`settings.json` to register them, and removes files. Pointed at a home that is
deny item three in its exact terms.
