# Source changelist: the unboxed arms each get a wire test

**Date:** 2026-09-04
**Phase:** IMPL
**Doc changelist:** `202609040030_changelist.doc.lock.md`

## `homma-core`

### `src/forge/github_wire_tests.rs` (modify)

CHANGE: `status_by_path` takes a body per route, and two tests are added:
`a_422_on_create_is_already_exists_only_when_the_body_says_so`, driving
`create_repo`'s 422 arm to `RepoAlreadyExists` on a body naming the
duplicate and to `UnexpectedStatus` on one that does not; and
`a_credential_is_rejected_on_401_and_recognised_on_403`, driving
`credential_works` through 401, 403 and a 500 control.
Verification: `cargo test -p homma-core forge::` lists both by name and is
green; commenting out the `ureq::Error::Status(422, resp)` arm in
`github.rs` turns the first red.

### `src/forge/forgejo_wire_tests.rs` (modify)

CHANGE: `a_409_on_create_is_already_exists_whatever_the_body` drives
`create_repo`'s 409 arm, and
`a_credential_is_rejected_on_401_and_recognised_on_403` drives
`credential_works` the same way as GitHub's.
Verification: the same run lists both and is green.

## `homma-engine`

### `src/cmd/aggregate.rs` (modify)

CHANGE: the doc comment on `is_aggregated_command` is rewrapped under 80
columns and names both shapes `is_retired_aggregated_command` owns.
Verification: `awk 'length > 80' mock/crates/homma-engine/src/cmd/aggregate.rs`
prints only the three licence header lines.
