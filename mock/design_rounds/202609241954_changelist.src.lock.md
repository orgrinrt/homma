# src changelist

Doc changelist: `202609241954_changelist.doc.lock.md`.

## Changes

- `mock/crates/homma-engine/src/cmd/capture/transcript.rs`: one `words` reader
  for a string or content blocks, used by typed lines and queued messages; the
  twin rule over every human line by `uuid` and `source_uuid`; `Question`
  carries `multi`, and `answered` takes it, splitting labels-first only when it
  is set.
- `mock/crates/homma-engine/src/cli.rs`: the `--since` help.
- Tests: blocks in a queued message, two attachments as one message, single
  against multi select; the smoke test makes its own link; the newest-transcript
  test sets modification times; `a_replayed_line_is_read_once` goes.
