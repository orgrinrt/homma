# Src changelist: what the second review of the hooks round found

Topic: `202609252350_topic.second-review-of-the-per-clone-hooks-round.md`. Doc
changelist: `202609252351_changelist.doc.lock.md`.

## CHANGE: fn `plain` FROM a comment being ` #` only and a value quoted whole whenever it starts and ends with a quote TO a `#` after any whitespace, and quoted whole only where the quote does not recur inside save escaped
File: `mock/crates/homma-org/src/agents.rs`
Verification: `a_comment_after_either_key_is_refused` gains a tab before the
`#` and two values that only look quoted, on both keys;
`a_hash_that_is_not_a_comment_is_read_as_text` gains `\"` inside double quotes
and `''` inside single ones as its controls.

## CHANGE: the folded-block fixture FROM its indented line after the body TO inside the frontmatter
File: `mock/crates/homma-org/tests/agents_corpus.rs`
Verification: `a_folded_or_literal_block_on_either_key_is_refused` builds the
line through `with_line`.

## CHANGE: a new arm pinning the name shape as homma's with no file present
File: `mock/crates/homma-engine/src/cmd/aggregate_settings_shared_tests.rs`
Verification: `a_hand_written_name_in_hommas_shape_with_no_file_is_swept_as_homma_s`,
beside `a_file_under_hommas_shape_that_homma_did_not_write_keeps_its_shared_registration`
as the case that keeps one.

## CHANGE: `docs/DESIGN.md` FROM as rendered before round `202609252212` TO regenerated
File: `docs/DESIGN.md`
Verification: `grep -c 'whichever member it names' docs/DESIGN.md` returns 1.

## What ran

On the lifebook at `d0fd493`, `cargo test --no-fail-fast -p homma-org -p
homma-engine`: every binary green, 610 passed in all, 323 in the engine's unit
tests with one ignored, 18 in `agents_corpus`. The control was a throwaway
commit restoring the value check as it was before this round:
`agents_corpus` went to 17 passed and 1 failed, the one being
`a_comment_after_either_key_is_refused`. The commit and its branch are gone.
`cargo mock` there regenerated `docs/DESIGN.md`, applied here as a patch.
