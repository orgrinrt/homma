# doc changelist: the stubs share one request reader

## Current state

The design says what the forge clients map each status to and nothing about
the stub servers their wire tests run against. Round `202609181236` said the
same and changed no document.

## Changes proposed

None. The reader is a test helper in `homma-core`'s forge module, behind
`cfg(test)`, and the repair of the previous round's empty source changelist is
a record in this round's source changelist rather than a design change. The
topic carries the reasoning.
