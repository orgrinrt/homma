# Source changelist: every hook refusal is reported, not thrown

**Date:** 2026-09-03
**Phase:** IMPL
**Doc changelist:** `202609030610_changelist.doc.lock.md`

## `homma-engine`

### `src/cmd/release/mod.rs` (modify)

CHANGE: `hook_cmd` reports `HooksPathTracked` and `HookExists` the way it reports `HooksPathOutside`, a line and a non-zero exit, rather than throwing them.

## `homma-core`

### `src/release/hook.rs` (modify)

CHANGE: a test plants a repo with a committed file and an untracked `.githooks` hooks path and asserts the install writes, the negative control on the tracked-directory check.
