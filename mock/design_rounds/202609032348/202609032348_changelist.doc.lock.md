# Doc changelist: the poster knows its workspace and records giving up

**Date:** 2026-09-03
**Phase:** DOC
**Source topics:** `202609032347_topic.the-poster-knows-its-workspace-and-records-giving-up.md`

## Current state

The poster paragraph in the release deep dive names the poster's command
without a config, says it gives up "saying so" with no surface named, says
the commit read is one path on both forges, and says nothing about a 404
that is the repository rather than the commit, or about when in the hook's
run the posters are left.

No other flat topic is open on this branch.

## Changes proposed

### `homma-core/DEEPDIVE_release.md.tmpl` (modify)

The poster paragraph says the poster is handed the manifest as an absolute
`--config`, and why; that it pauses after every miss and gives up after ten
minutes of asking by appending a `poster-gave-up` record beside the runs,
carrying the repository, the sha, when and how long; that the read is
`commits/{sha}` on GitHub answering 422 and `git/commits/{sha}` on Forgejo
answering 404; that a repository-level 404 ends the wait as an error, with
how each forge's 404 is told apart; and that posters are left after every
tip has been gated and only when every tip is green.

## Lock criteria

The paragraph names the config, the record kind and what it carries, both
paths with the status each forge answers, the 404 rule per forge, and when
posters are left.
