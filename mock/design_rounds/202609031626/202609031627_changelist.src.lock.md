# Source changelist: a content repo's gate summary says so

**Date:** 2026-09-03
**Phase:** IMPL
**Doc changelist:** `202609031626_changelist.doc.lock.md`

## `homma-api`

### `src/release.rs` (modify)

CHANGE: `GateRun::summary` returns `nothing to run: every step skipped, the repository's markers say only content` when the run has steps and every one is skipped, and is unchanged otherwise. Tests pin the all-skipped run, a run with one step that ran keeping its old summary with the skipped ones left out, and the empty run summarising as the empty string it always did, which is the control.

