# src changelist

The source half of the fix round, already committed ahead of the topic since
the launcher sits outside the phase gate; this records it. Descends from the
locked doc changelist `202609041854_changelist.doc.lock.md`.

## CHANGE: `reap` FROM `survey, then remove` TO `four refusals ahead of the survey that --force does not lift`
File: `launcher/src/workspace/reap.rs`, `launcher/src/workspace/mod.rs`
Verification: `grep -c 'not a workspace and is not removed\|refusal_for(root, home)\|the cwd is inside\|fn worktrees_in' launcher/src/workspace/reap.rs` is 4; `cargo test --test the_workspace_command_runs_where_there_is_no_workspace reap_` is green over two tests, the worktree one made by `git worktree add` under `.worktrees/` and one level down, each refused under `--force`, with the control that the reap goes through once the seats are gone.

## CHANGE: `Prefs::read` FROM `takes workspaces_root as given` TO `refuses a relative one`
File: `launcher/src/settings.rs`
Verification: `grep -n 'is_absolute()' launcher/src/settings.rs` returns one match inside `read`; the binary test's `--cfg workspaces_root=ws` arm is green and asserts nothing was made.

## CHANGE: `resolve_through_ancestors` FROM `gives up with the caller's spelling` TO `gives up with the folded path`
File: `launcher/src/settings.rs`
Verification: `grep -n 'lexical_of(path)$' launcher/src/settings.rs` returns one match at the end of the function; `cargo test a_path_that_is_not_there_yet` is green with the relative give-up arm in it.

## CHANGE: `spawn` FROM `leaves a half-made workspace on a failed member` TO `removes what it made, or empties what it was handed, and says so`
File: `launcher/src/workspace/spawn.rs`
Verification: `grep -n 'fn clone_all\|nothing is left at' launcher/src/workspace/spawn.rs` returns one of each; `cargo test --test the_workspace_command_runs_where_there_is_no_workspace a_spawn_that_fails_partway` is green over both the created and the handed directory.

## CHANGE: `preflight` FROM `the missing key ahead of the denied root` TO `the denied root first`
File: `launcher/src/workspace/spawn.rs`
Verification: `cargo test --test the_workspace_command_runs_where_there_is_no_workspace the_denied_root_is_named_ahead` is green.

## CHANGE: the git module's doc FROM `one dependency` TO `renki and the two crates beside it`
File: `launcher/src/workspace/git.rs`
Verification: `grep -c 'one dependency' launcher/src/workspace/git.rs` is 0.

## Not changed, and why

The commit subject the review counted past 72 characters is pushed and
stays. The closed round's topic still carries the one-dependency sentence,
being an archive; this round's topic says so.
