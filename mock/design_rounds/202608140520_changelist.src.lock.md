# Changelist: src — the standing instruction gains a branch rule

**Round:** 202608140500 what the first Hand got wrong
**Phase:** IMPL
**Scope:** one addition to the generated discipline, and the test that pins it

## CHANGE: forbid committing to a trunk

The standing instruction every generated definition carries now says to branch
first and never write to `dev` or `main` directly.

**Verification:** a test asserts the constant contains the prohibition, and the
regenerated definition was read back from disk before this was committed. The
second end-to-end run committed on `chore/paja-first-memory` rather than `main`,
staged only its own two paths, and left the in-flight standup files alone without
being asked to.
