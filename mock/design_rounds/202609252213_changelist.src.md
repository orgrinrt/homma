# Src changelist: what the review of the per-clone hooks round found

Topic: `202609252210_topic.review-of-the-per-clone-hooks-round.md`. Doc
changelist: `202609252212_changelist.doc.lock.md`.

## CHANGE: fn `merge_settings` FROM sweeping and writing the shared file before the local one is read TO reading and sweeping both, then writing the local one, then the shared one if it was swept
File: `mock/crates/homma-engine/src/cmd/aggregate_settings.rs`
Verification: `a_local_file_that_refuses_leaves_the_shared_one_as_it_was` in
`aggregate_settings_shared_tests.rs`, four local files that refuse, each
asserting the shared file byte for byte; its control
`a_local_file_that_parses_lets_the_same_shared_file_be_swept`.

## CHANGE: the shared sweep's predicate FROM the local file's, scoped to the visited repos TO homma's shape whatever repo it names, still keeping a managed name homma did not write
File: `mock/crates/homma-engine/src/cmd/aggregate_settings.rs`, new fn
`is_any_aggregated_command`
Verification: `the_shared_file_is_swept_of_every_repos_registrations_visited_or_not`
(an unvisited repo, an undeclared one, the gate and a declared hook, beside
one of somebody's own), `an_unvisited_repos_registration_is_kept_in_the_local_file_and_swept_from_the_shared_one`,
and `a_file_under_hommas_shape_that_homma_did_not_write_keeps_its_shared_registration`
with `--odd.sh` and `odd--` as names that only look like the shape.

## CHANGE: fn `declared` FROM reading any value after the colon TO refusing a folded or literal block and a comment on `name` or `description`, as the new `AgentsError::BadValue`
File: `mock/crates/homma-org/src/agents.rs`, new fn `plain`
Verification: `a_folded_or_literal_block_on_either_key_is_refused`,
`a_comment_after_either_key_is_refused`, their control
`a_hash_that_is_not_a_comment_is_read_as_text`, and
`a_nested_line_shaped_like_a_bad_value_is_not_read`, all in
`homma-org/tests/agents_corpus.rs`.

## CHANGE: fn `unquote` in `agents.rs` FROM a local copy TO `homma_api::frontmatter::unquote`
File: `mock/crates/homma-org/src/agents.rs`
Verification: `grep -c 'fn unquote' mock/crates/homma-org/src/agents.rs`
returns 0; `a_quoted_name_is_read_without_its_quotes` still passes.

## CHANGE: the render-refusal arm FROM a broken persona sorting first TO one sorting last, with the order asserted
File: `mock/crates/homma-org/tests/agents_corpus.rs`
Verification: `a_template_that_does_not_render_is_refused_and_nothing_is_written`
asserts the order `reviewer`, `zz-broken`, and fails against a `render` that
writes each persona as it goes, which was run once as the control.

## CHANGE: the module doc of `agents.rs` FROM silent on the trailing newline TO saying a generated persona ends in exactly one
File: `mock/crates/homma-org/src/agents.rs`
Verification: `a_generated_persona_ends_in_exactly_one_newline`, over four
template tails.

## CHANGE: four comments FROM saying the wrappers or the gate are tracked TO saying they are generated per clone
Files: `mock/crates/homma-engine/src/cmd/agent.rs`,
`mock/crates/homma-engine/src/cmd/gates.rs` (two),
`mock/crates/homma-engine/src/cmd/aggregate_wrapper.rs`
Verification: `grep -rn 'tracked' mock/crates/homma-engine/src/cmd/` names
only `release/order.rs`, which is about something else.

## CHANGE: rustdoc on `Personas::personas`, `authored_dir` and `load`
Files: `mock/crates/homma-org/src/agents.rs`,
`mock/crates/homma-engine/src/cmd/agents.rs`
Verification: each carries a `///` line.
