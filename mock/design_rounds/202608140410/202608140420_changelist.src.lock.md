# Changelist: src — none

**Round:** 202608140400 the docs must name the types
**Phase:** IMPL
**Scope:** nothing

No source changes. The round exists because three public types were undocumented,
and the fix is entirely in the documents.

The changelist exists anyway because a round reaches CLOSED through a locked
source changelist, and a doc-only round that is left in DRAFT is an open round
that nothing will ever close. Saying "none" here is cheaper than an untidy
archive.

**Verification:** `git diff --cached -M --numstat -- 'mock/crates/*/src'` is
empty for this round's commits.
