# Changelist: doc — name the types

**Round:** 202608140400 the docs must name the types
**Phase:** DOC
**Scope:** three type names in two crate documents

## CHANGE: `homma-store` names `Store` and `Error`

The document describes a store and what refusing a write means, without naming
either type. Adds a short surface section.

## CHANGE: `homma-org` names `Registry`

Same shape: the document describes the registry as a concept and never names the
type that reads it.
