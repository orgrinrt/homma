# Source changelist: the tagline scan stops at the first section

**Date:** 2026-09-03
**Phase:** IMPL
**Doc changelist:** `202609032254_changelist.doc.lock.md`

## `homma-core`

### `src/release/tagline.rs` (modify)

CHANGE: `tagline(readme)` FROM a scan to the end of the file TO a scan
between the first heading and the next, with fenced code skipped and a fence
ending a quote in hand.
Verification: `the_search_stops_at_the_first_section_and_skips_fenced_code`
in `tagline_tests.rs` is red on the previous revision and green here.

CHANGE: `unwalked(name)` (add) names the directories `published` leaves:
`target`, `node_modules`, `.git`, `research`, `design_rounds` and anything
ending in `probes`.
Verification: `grep -c "fn unwalked" mock/crates/homma-core/src/release/tagline.rs` is 1.

### `src/release/tagline_tests.rs` (add)

CHANGE: the tests move out of `tagline.rs` behind `#[path]`, the way
`gate_tests.rs` sits beside `gate.rs`, and gain the later-quote, fenced-code
and audit-trail cases.

## `launcher`

### `Cargo.toml` (modify)

CHANGE: `description` FROM the launcher's own sentence TO the readme's
tagline, so the step passes on the tree that ships it.
Verification: `homma release gate` on the tip reports the description step
green.
