# Source changelist: the sweep refuses what a single run refuses

**Date:** 2026-09-03
**Phase:** IMPL
**Doc changelist:** `202609030250_changelist.doc.lock.md`

## `homma-engine`

### `src/cmd/release/mod.rs` (modify)

CHANGE: the sibling refusal passes `active` without the repo's own name; a `PlanError::OffLevel` in the sweep's pre-pass refuses the run with the repo named, instead of passing the repo over.

### `src/cmd/release/order.rs` (modify)

CHANGE: a test plants a workspace whose member crate is named for the repo and depends on a sibling member of the same workspace, and asserts `sibling_dependency` returns nothing when the repo's own name is absent from the slice and the repo's name when it is present, which is the case the caller now avoids.

## `homma-api`

### `src/release.rs` (modify), `src/release_tests.rs` (new)

CHANGE: the inline tests move to `release_tests.rs` through `#[path]`, so the module sits under the file-size line. No item changes.
