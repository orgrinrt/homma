# Changelist: src — none

**Round:** 202608140700 the readme describes a repository that no longer exists
**Phase:** IMPL
**Scope:** nothing

No source changes. The round is a readme and one character of configuration.

**Verification:** `git diff --cached -M --numstat -- 'mock/crates/*/src'` is empty
for this round's commits.
