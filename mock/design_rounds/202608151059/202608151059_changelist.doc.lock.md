# Doc changelist: none

No `*.md.tmpl` edits this round. The two remaining instances of the
singular-identity class live in `//!` and `///` doc comments, which are source
files and are in the src changelist.

`homma-api/DESIGN.md.tmpl:61` and `:114` were checked and are deliberately left:
they say a Hand needs "a git identity" where a consultant needs none, which is a
claim about presence rather than about the lifecycle that sets it.
