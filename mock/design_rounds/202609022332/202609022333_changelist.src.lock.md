# Source changelist: what the fourth review found on the release

**Date:** 2026-09-02
**Phase:** IMPL
**Doc changelist:** `202609022332_changelist.doc.lock.md`

## `homma-core`

### `src/release/kind.rs` (modify)

CHANGE: the test pinning a subdirectory manifest as not counting becomes an ignored catalogue red asserting the opposite, `mock/Cargo.toml` names a crate workspace, tracked as `homma-release-reads-a-manifest-under-mock`; the positive test that the root is read stays.

### `src/release/check.rs`, `check_tests.rs` (modify)

CHANGE: `main.unreleased` clears only where every intervening subject carries `hotpatch` as a word that is not preceded by `not` or `no`; the test adds "this is not a hotpatch" as a subject that does not clear it.

### `src/release/git.rs` (modify), `git_tests.rs` (new)

CHANGE: the inline tests move to `git_tests.rs`, so the module sits under the file-size line. No function changes.

### `src/release/publish.rs` (modify)

CHANGE: `crate_dirs` is `pub`, so the engine's sibling refusal walks the same member listing the publish does.

### `src/release/run_tests.rs` (modify)

CHANGE: the dead `let _ = tip;` goes; the test asserts against `tip` that the trunk did not move.

## `homma-engine`

### `src/cmd/release/mod.rs`, `order.rs` (modify)

CHANGE: a workspace-wide `run` prints a line per repo it passes over, naming the repo and the reason (no root manifest, or nothing unreleased), instead of continuing in silence. `sibling_dependency` reads every crate directory `publish::crate_dirs` lists, not only the root manifest, with a test planting the edge in a member crate.
