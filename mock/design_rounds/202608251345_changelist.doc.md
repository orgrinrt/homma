# Changelist: doc — membership is detected

**Round:** 202608251338 membership is detected, not declared
**Phase:** DOC
**Scope:** the workspace design document

## CHANGE: say how homma knows which repositories the workspace contains

`mock/DESIGN.md.tmpl`.

The opening sentence claims homma "knows which repositories the workspace
contains" and never says how, which was fine while the answer was a table
somebody wrote. It stops being fine when the answer becomes a property of the
tree, because a reader who assumes a manifest will go looking for one.

The "General" paragraph gains the sentence that actually makes the claim true.
It already says no key is required and that nothing about the workspace homma
was born in is compiled in. Detection is what closes the gap between that claim
and the twenty-four-entry table sitting in the workspace it was born in.
