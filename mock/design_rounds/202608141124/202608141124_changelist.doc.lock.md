# DOC changelist: `local` is the default content repository

**Round:** the local round
**Phase:** DOC

## CHANGE: mock/crates/homma-org/DESIGN.md.tmpl

Say that the content repository is a git URI or `local`, that `local` is the
default and means the configuration's own directory, that homma initialises it
when it is not a repository yet, and that an existing one is left alone.

## CHANGE: mock/crates/homma-api/DESIGN.md.tmpl

The configuration section says one required key names the content repository.
There are now no required keys at all.
