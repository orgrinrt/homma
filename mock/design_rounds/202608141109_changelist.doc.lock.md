# DOC changelist: the root is not the cwd

**Round:** the root round
**Phase:** DOC

## CHANGE: mock/crates/homma-org/DESIGN.md.tmpl

Replace the paragraph claiming the clone URL comes from the root's `origin`
"never a configuration key". Both facts exist and the honest design uses both:
the URL comes from the origin and is **verified against `content_repo`**, and a
disagreement is refused naming both values.

Say that the root is the configuration file's own directory rather than the
current one, and why: the current directory is not a statement about which
workspace is being operated on, and treating it as one wrote a Hand's directories
into an unrelated repository.

Keep the sentence claiming the identity is asserted after standing up, and make
it true by reading it back, rather than deleting the claim.
