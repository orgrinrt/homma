# Changelist: doc, none

**Round:** 202608251452 the round shipped the other answer in three places
**Phase:** DOC
**Scope:** none

No template changes. `mock/DESIGN.md.tmpl` already carries the membership
paragraph; what is wrong is that `docs/DESIGN.md` was never rendered from it,
which the render does rather than an edit.

`README.md` is authored rather than generated and sits outside this window.
