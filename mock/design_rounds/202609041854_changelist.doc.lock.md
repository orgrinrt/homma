# doc changelist

The root design document says what `reap` refuses. Descends from the topic
file `202609041849_topic.the-review-findings-on-reap.md`.

## What lands

- `mock/DESIGN.md.tmpl`, under "The command surface": `reap` refuses
  outright a target that is not a workspace, one the settings deny, one the
  cwd is inside, or one holding a worktree.
- `README.md` at the repo root, hand-written: the `homma workspace` row says
  the same in its words.
- `docs/DESIGN.md`, rendered.

## Claims the source changelist will carry

The four refusals in `reap`, ahead of the survey; the absolute
`workspaces_root`; the resolver's give-up branch; the rollback in `spawn`;
the refusal order in `preflight`; the git module's doc; and the tests.
