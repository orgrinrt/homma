# doc changelist: a clone of the workspace says what it is for

## Current state

The design describes the workspace manifest, which every clone of the content
repository shares, and nothing that belongs to one clone alone. What a clone is
for is recorded only by the directory it sits in.

## Changes proposed

`mock/DESIGN.md.tmpl` gains a section, `What a clone is for`, carrying the
topic `202609241531_topic.a-clone-of-the-workspace-says-what-it-is-for.md`
whole: the file `homma.local.toml`, its two tables and which of them homma
types, where it loads and how a malformed one fails, the three `homma local`
verbs, and the instance appearing in `homma status`.

`What a status answers` names the instance among what the status covers.
