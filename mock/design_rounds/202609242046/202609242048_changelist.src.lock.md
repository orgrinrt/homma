# source changelist

Implements `202609242046_changelist.doc.lock.md`.

## CHANGE: struct `Paths` FROM five paths TO six, adding `captures`
File: `mock/crates/homma-api/src/config.rs`
Verification: `grep -n 'captures' mock/crates/homma-api/src/config.rs` shows the
field and its default `.data/op-responses`; a test parses `[paths] captures`.

## CHANGE: const `STORE` in `capture/store.rs` FROM the store's path TO removed
File: `mock/crates/homma-engine/src/cmd/capture/store.rs`
Verification: `grep -rn 'STORE' mock/crates/homma-engine/src/cmd/capture` finds
nothing; `Ask` carries the configured store and the dispatcher reads it from the
manifest's `[paths]`.

## CHANGE: fn `store::transcript` FROM newest-or-named in one directory TO named, then running session, then newest, an id searched across every project directory
File: `mock/crates/homma-engine/src/cmd/capture/store.rs`
Verification: tests for an id under another project directory, an id found
nowhere refused, and the running session chosen over a newer transcript.

## CHANGE: fn `transcript::read` FROM any result with `answers` TO only results whose `tool_use_id` names an `AskUserQuestion` call
File: `mock/crates/homma-engine/src/cmd/capture/transcript.rs`
Verification: a test where another tool's result carries `answers` and no
`questions`, read without error and without a round; the existing round tests
carry the assistant call.

## CHANGE: fn `render::one_line` added, used for the title, headers and labels
File: `mock/crates/homma-engine/src/cmd/capture/render.rs`
Verification: a test putting `\n> ` into all six places agent text can sit, and
finding no quote but the person's.

## CHANGE: fn `store::slug` FROM ASCII only TO folding `ä`, `å`, `ö`
File: `mock/crates/homma-engine/src/cmd/capture/store.rs`
Verification: `slug("Käyttö")` is `kaytto`.

## Tests and wording

- The exhaustive quote round trip over short strings becomes a test.
- The smoke test's symlink is gated on `unix`, which is the module it uses.
- `his`, `he` and the process words in tests and comments go.
