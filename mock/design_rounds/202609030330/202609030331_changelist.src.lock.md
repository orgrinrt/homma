# Source changelist: a fresh clone still merges dev onto main

**Date:** 2026-09-03
**Phase:** IMPL
**Doc changelist:** `202609030330_changelist.doc.lock.md`

## `homma-core`

### `src/release/check.rs`, `check_tests.rs` (modify)

CHANGE: `both.sameset` pairs the jsr name and the npm name the repo's own manifests declare, through `packages`, and looks each up in what is published; the bare-name pairing goes. The test plants `deno.json` and `package.json` in the fixture with names sharing no segment and asserts the pair is compared.

### `src/release/gate.rs`, `gate_tests.rs` (modify)

CHANGE: the wall time is filed on the last step that ran rather than the last step listed; the test asserts it sits on a step that is not skipped where the notices step is.

## `homma-engine`

### `src/cmd/release/mod.rs` (modify)

CHANGE: `branches_for` reads the working branch as present where either the local ref or `origin/<working>` resolves, and `branches_checked`, which every subcommand goes through, refuses where it is on origin and not checked out here, naming `git switch <working>`, since the plan reads that branch and the run commits on it. `plan_cmd` opens its report with the branches: the working branch onto the release line, or the release line alone.

### `tests/cli_smoke.rs` (modify), `tests/support/mod.rs` (modify), `tests/cli_release.rs` (new)

CHANGE: the release smoke tests move out of `cli_smoke.rs` into `cli_release.rs`, and the fixtures both share, the binary, the smallest manifest, a clone, a committed crate and a git runner, into the existing `support` module, bringing `cli_smoke.rs` under the file-size line. A new test clones a repo with `origin/dev` and no local `dev`, asserts the plan refuses naming `git switch dev`, checks it out and asserts the plan says `dev` onto `main`, then deletes `dev` everywhere and asserts `main` alone.
