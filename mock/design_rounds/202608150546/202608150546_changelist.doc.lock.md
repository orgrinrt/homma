# Doc changelist: the identity is two, in every document that says otherwise

## CHANGE: `README.md`, the line the previous round overran

Ninety-nine characters became one hundred and fifty-one. Rewrapped to the
paragraph's own width, which its neighbours hold at 78 to 81.

## CHANGE: `homma-org/DESIGN.md.tmpl`, two claims of one identity

Line 9 enumerates what an entry carries and names "a git name and email". An
entry carries an author name and address, and optionally a committer name and
address.

Line 163's paragraph says the clone "reports the identity's own email". The
read-back compares four values, and the reason it does is the thing that
paragraph is about.

## What is checked and left alone

`homma-api/DESIGN.md.tmpl` says "a git identity" in two places. Both stay: an
identity is a git identity whether or not it has two halves, and the sentences
are about whether a role has one at all.

The class grep found four candidates. It does not decide them.
