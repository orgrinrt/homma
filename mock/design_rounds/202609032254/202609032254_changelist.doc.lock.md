# Doc changelist: the tagline scan stops at the first section

**Date:** 2026-09-03
**Phase:** DOC
**Source topics:** `202609032254_topic.the-tagline-scan-stops-at-the-first-section.md`

## Current state

The description step's paragraph says the tagline is the first `>` line
after the first heading and that the opening prose is never taken for it,
without saying where the search stops; the code runs it to the end of the
file and reads a fenced block beginning `>` as a quote. The manifest walk
leaves only `target/`, `node_modules/` and `.git/` unwalked, so a mockspace
repository's committed research probes are read as published crates.

No other flat topic is open on this branch.

## Changes proposed

### `homma-core/DEEPDIVE_release.md.tmpl` (modify)

The paragraph under the step table says the blockquote is looked for
between the first heading and the next heading, with fenced code skipped,
and that nothing below the first section is read; and that the walk also
leaves any directory named `research` or `design_rounds` or ending in
`probes` unwalked, being the audit trail.

## Lock criteria

The paragraph names the bound of the search and the directories the walk
leaves, in one sentence each.
