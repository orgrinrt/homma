# Doc changelist: the guard belongs on the write

No `*.md.tmpl` edits. The design statement lives in the topic file and in the
module documentation the source changelist adds, because what changes is a type
and its contract rather than anything a rendered document describes today.

Opened and locked so the round walks its phases honestly rather than skipping to
source.
