# DOC changelist: a relative path walks out of the guard

**Round:** the relative-path round
**Phase:** DOC

## CHANGE: mock/crates/homma-api/DESIGN.md.tmpl

State that a path reaching the git contract is absolute, and why: a relative one
resolves against whatever directory the process happens to be in, which is the
thing the workspace root already stopped being derived from.

State that absence of a repository is `None` rather than an error, since the
contract asks what a remote points at "if it has one".
