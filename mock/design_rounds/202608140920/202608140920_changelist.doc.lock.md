# DOC changelist: standing one up is one command

**Round:** 202608140910
**Phase:** DOC

## CHANGE: mock/crates/homma-api/DESIGN.md.tmpl

State the mapped-versus-staffed distinction as part of what an identity is, since
it is a property of the vocabulary rather than of any command. Say why the mark is
explicit rather than inferred from an absent workspace.

## CHANGE: mock/crates/homma-org/DESIGN.md.tmpl

Extend what standing up covers: the clone and the per-clone git identity, and the
fact that cloning is skipped when the repository is already there so that standing
up twice stays the same answer. Say that a mapped identity is refused rather than
promoted.
