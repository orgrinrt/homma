The surface a human or an agent drives: the `homma` binary and its command
families. `status`, `verify`, `repo`, `forge`, `org`, `agent`, `docs`, `migrate`
and `archive` read and move the workspace; `release` gates a commit, records the
run, and carries `dev` onto `main` as a tagged, published release.
